[project]
name = "app"
version = "0.1.0"
description = ""
authors = [
    {name = "OG"}
]
license = "MIT"
readme = "README.md"
requires-python = "^3.11"
dependencies = [
    "langgraph (>=1.2.4,<2.0.0)",
    "langchain-openai (>=1.3.0,<2.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "langgraph-cli[inmem] (>=0.4.28,<0.5.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
